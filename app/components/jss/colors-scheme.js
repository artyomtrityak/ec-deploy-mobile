'use strict';

import Immutable from 'immutable';

export default Immutable.Map({
  lightGray: '#CCCCCC',
  gray: 'gray',
  darkGray: '#676767',
  white: '#FFFFFF',
  separatorColor: 'rgba(0, 0, 0, 0.1)'
});